"""Servicios de infraestructura del backend (almacenamiento de archivos, etc.).

Por ahora solo `storage` (guarda el PDF original en disco). Se mantiene como paquete propio
para separar la persistencia de archivos de la lógica de ingesta (`app.ingestion`).
"""
